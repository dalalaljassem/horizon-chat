import 'package:flutter/material.dart';

class AppColors {
  // Background colors
  static const Color mainBackground = Color(0xFFF6F6F6);
  static const Color overlayBackground = Color(0xFFEDF0F5);
  static const Color white = Colors.white;

  // Blur circle colors
  static const Color blurPurple = Color(0xFFE8D5F2);
  static const Color blurBlue = Color(0xFFDAE4F0);
  static const Color blurGray = Color(0xFFD8E3ED);

  // Sparkle icon
  static const Color sparkleBlue = Color(0xFF4F7CFF);

  // AI Assistant
  static const Color aiAvatarBackground = Color(0xFFE8EEF7);

  // Text colors
  static Color textPrimary = Colors.grey[900]!;
  static Color textSecondary = Colors.grey[600]!;
  static Color textTertiary = Colors.grey[800]!;
  static Color textHint = Colors.grey[400]!;

  // Component colors
  static Color inputBackground = Colors.grey[100]!;
  static Color messageBubbleUser = Colors.grey[200]!;
  static const Color sendButton = Colors.blue;
}
